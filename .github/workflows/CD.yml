# name: CD - Deploy to Railway

# on:
#   push:
#     branches: [main, develop]
#   workflow_dispatch:

# concurrency:
#   group: deploy-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     # Optional: if CI workflow name is "Node.js CI with Secrets", you can use needs via workflow_run pattern instead.
#     steps:
#       - uses: actions/checkout@v5

#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20.x"
#           cache: "npm"

#       - run: npm ci
#       - run: npm test -- --runInBand

#       - name: Install Railway CLI
#         run: npm i -g @railway/cli

#       - name: Deploy to Railway (staging on develop, production on main)
#         env:
#           RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
#           RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
#           RAILWAY_SERVICE_ID: ${{ secrets.RAILWAY_SERVICE_ID }}
#           RAILWAY_ENV_PROD: ${{ secrets.RAILWAY_ENV_PROD }}
#           RAILWAY_ENV_STAGING: ${{ secrets.RAILWAY_ENV_STAGING }}
#         run: |
#           if [ "${GITHUB_REF_NAME}" = "main" ]; then
#             railway up --ci --service "$RAILWAY_SERVICE_ID" --project "$RAILWAY_PROJECT_ID" --environment "$RAILWAY_ENV_PROD"
#           else
#             railway up --ci --service "$RAILWAY_SERVICE_ID" --project "$RAILWAY_PROJECT_ID" --environment "$RAILWAY_ENV_STAGING"
#           fi

#       - name: Smoke check
#         env:
#           PROD_URL: ${{ secrets.PROD_BASE_URL }}
#           STAGING_URL: ${{ secrets.STAGING_BASE_URL }}
#         run: |
#           URL="$STAGING_URL"
#           if [ "${GITHUB_REF_NAME}" = "main" ]; then URL="$PROD_URL"; fi
#           curl -f "$URL/health"